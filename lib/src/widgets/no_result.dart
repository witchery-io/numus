import 'package:flutter/material.dart';

class NoResult extends StatelessWidget {
  NoResult({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('No result')));
  }
}
